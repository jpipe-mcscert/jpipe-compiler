package ca.mcscert.jpipe.operators;

/**
 * Discriminates between the two concrete subtypes of {@code JustificationModel}
 * that a {@link CompositionOperator} can produce.
 */
public enum ModelKind {
	JUSTIFICATION, TEMPLATE
}
